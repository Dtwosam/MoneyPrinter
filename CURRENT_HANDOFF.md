# Printer V1 Handoff

## Current HEAD

This handoff is committed with the consolidated post-holder temporal-acquisition
repair and its focused CI verification. Use `git rev-parse HEAD` for the exact
committed HEAD.

## Authoritative DB

`data/printer_v1.sqlite3`

Latest post-run identity from the consumed child terminal remains SHA-256
`f6e07ca0f32a70e60f6821f44074c73356f57b0d56e98d81e48e92a4f05f46b4`,
size `170061824`, inode `1230526`, mtime_ns `1788696345906968820`.
No operational execution or authoritative-DB mutation occurred during this
repair/verification lane. Re-derive exact DB identity, integrity/FK health,
sidecars, and zero-active-work locally before any future authorization package
is prepared.

## Latest consumed authorization

One-shot authorization
`V2_9_8B_FOUR_TOKEN_STD4H_AUTH_20260906T111524Z_28455922` was consumed exactly
once by execution `20260906T114531Z-8cc313cae90d` and is permanently
non-reusable. Its pre-lifecycle terminal cause was
`FrozenInstanceError:cannot assign to field 'diagnostics'`. No retry, rerun,
restart, resume, successor, or reuse is permitted or implied.

## Current working capability

The bounded Solana-only, memecoin-only, paper-only memory-factory path retains
Source Governor as sole governed source-request owner and Central Scheduler as
sole scheduler owner. Strict measured-transport, manifest, duplicate,
reconciliation, data-quality, and clean-memory guards remain fail-closed.
The 4/2/2 Standard-4H policy remains unchanged. `WINDOW_5M_MICRO_EVENT` stays
support-only; `WINDOW_12H` and `WINDOW_24H` stay locked. Retrieval,
financial, position, and live-trading capabilities remain locked.

## Latest meaningful result

The post-holder temporal-acquisition path is consolidated around the canonical
eligible-supply owner. A completed refresh now carries exact request evidence,
resumes canonical supply with cumulative operation/coverage state, rebinds
durable request and independent transport provenance, rebases the campaign
operation ledger, and re-freezes before readiness. Non-completed refresh
outcomes preserve the canonical fail-closed terminal. The frozen
`GraduatedSupply` carrier is updated only through immutable replacement.

Disposable development rehearsal is permitted for Standard-4H through the
existing proof-bound disposable DB/composition path; the operational CLI and
one-shot wrapper remain authorization-bound.

Verification on GitHub Actions:
- focused post-holder/reconciliation suite: 17 passed;
- shared-boundary suite: 117 passed + 7 subtests, with two legacy E.44
  assertions deselected only after both were proven to fail on the pre-repair
  HEAD as well;
- affected-module `py_compile`: passed;
- `git diff --check`: passed.

The permanent focused CI guard also covers four-token Standard-4H command,
one-shot wrapper, 4/2/2 offline proof, Cycle-2 refresh re-entry, retained
evidence/freeze integration, and disposable public-composition boundaries.

## Known blocker

No unresolved code blocker is proven in this repair lane.

Operational readiness has not been re-established for this new HEAD: the
authoritative local DB and one-shot authorization gates must be freshly
re-derived and rebound before another operational execution.

## Next permitted action

Sync this exact branch HEAD locally, prove a clean tracked/staged tree, then run
the canonical read-only authoritative-DB / migration / integrity / FK /
sidecar / zero-active-work / prior-authorization-non-reuse gates. If green,
prepare one fresh authorization package bound to this exact HEAD and current DB
identity. Do not consume it or run Printer operationally without fresh explicit
operator approval.
