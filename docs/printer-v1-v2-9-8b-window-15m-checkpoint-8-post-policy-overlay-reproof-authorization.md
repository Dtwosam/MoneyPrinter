# Printer V1 V2-9.8B WINDOW_15M Checkpoint 8 — Post-Policy-Overlay Re-proof Authorization

Date: 2026-08-07

Readiness HEAD: `82f6a913f99c5744610d5ec34cc0c42257b9e6ae`

## Explicit operator authorization

The operator explicitly authorized exactly one new ordinary `WINDOW_15M` Checkpoint 8 re-proof attempt after the proof-bridge operational-policy overlay repair.

Authorization wording received:

`I explicitly authorize one new Checkpoint 8 re-proof attempt after the proof-bridge operational-policy overlay repair.`

## Scope

This authorization permits exactly one fresh controlling proof attempt using:

- the exact authorized Git HEAD produced by this documentation-only commit;
- a fresh disposable SQLite database;
- a fresh disposable artifact root;
- a fresh proof identity and sentinel namespace;
- deterministic zero-provider fixture transports;
- the canonical permanent graduated-token supply policy with C8 fixture dependencies overlaid;
- the existing campaign-pass gate before independent success inspection.

## One-shot law

The authorization is consumed when the one-shot runner starts. It does not permit retry, rerun, resume, restart, or successor execution.

Independent success inspection is permitted only when frozen `campaign_pass == true` and `campaign_acceptance_verdict == CAMPAIGN_PASS`.

## Locks preserved

No `WINDOW_1H+`, retrieval, paper decisions, BUY/SELL/HOLD, positions, trades, audits, or PnL are unlocked by this authorization.
