# V2-9.7D.6B.8 Isolated Slice 6 Integration Proof Closeout

## Scope

This lane composes the completed Slice 6 owners in one disposable database. It
uses exactly two campaign token slots in each of two cycles and invokes no
operational command, real source, live runtime, retrieval, or financial path.

## Money-Usefulness Contribution

The proof demonstrates that selective memory growth can preserve clean
promotion authority, safety context, token-local isolation, event-time gaps,
terminal cleanup, and reproducible reporting without manufacturing trades or
contaminating locked tables. It closes the static-integration risk between the
individual Slice 6 lanes before any operator-facing activation is considered.

## Proof Results

- Operational backup publication and disposable restore rehearsal preserve the
  source and include required migration 032 plus current migration 033.
- One campaign/run owns two cycles with exactly two slots per cycle and the
  same two token identities remain isolated.
- Fairness serves both tokens; a token-local blocker excludes only that token;
  a shared integrity failure blocks shared work.
- Token 1 continues 15m to 1h to 4h while token 2 stops after 15m. A later
  token-local safety blocker does not stop token 1.
- Positive capture and valid negative/no-capture 5m paths remain support-only
  and have no continuation authority.
- Required 4A-5C immutable objects are stored and exact-linked. B.1 returns
  clean promotion and no-promotion independently; B.2 preserves raw labels and
  accepted effective context for 15m, 1h, and 4h.
- B.3 creates exact terminal dispositions, clears associated work, and permits
  replacement only after reconciliation.
- Campaign supervision acquires one lease, records cooperative cancellation
  and the shared first fault, clears all owned work, releases the lease, and
  creates no resume, successor, or restart.
- Final-report bytes/hash are deterministic and exactly-once persistence is
  idempotent. Zero-source replay verifies with unchanged DB hash/counts and
  `total_changes=0`.
- Locked-capability baseline/final/current counts and deltas are exactly zero.

## Remaining Locks

Persistent-target migration, command surfaces, operational campaign runtime,
real source fetching, memory activation, retrieval, decisions, BUY/SELL/HOLD,
positions, trades, audits, PnL, wallets, signing, and live execution remain
locked. This fixture is proof-only and does not authorize activation.

## Functionality Risks / Setbacks / Efficiency Blockers

- The proof intentionally uses dense synthetic authoritative rows; it validates
  contracts and ownership, not provider availability or long-running behavior.
- Backup rehearsal now distinguishes the required migration floor (032) from
  the actual latest migration (033). Future migrations must remain ledger-
  validated rather than restoring an obsolete exact-latest assumption.
- Whole-file replay verification requires a quiescent terminal database and
  fails closed if another writer changes it.
- Candidate replacement eligibility is proven as a read-only gate. No
  replacement slot or successor campaign is created.

## Stop Boundary

V2-9.7D.6B.8 ends here. No command surface, persistent migration, or
operational campaign is started.
